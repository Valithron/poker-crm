{
  "name": "brotm-poker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Fresh Cloudflare Pages foundation for BroTM Poker.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/validate.mjs",
    "check": "npm run test && npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
